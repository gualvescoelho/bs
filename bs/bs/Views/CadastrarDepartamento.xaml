<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-ViewModels:bs"
             Title="{Binding Title}"
             x:Class="bs.ViewModels.CadastrarDepartamento">
    <ContentPage.Content>
        <StackLayout>
            <Entry Text="{Binding Nome}" Placeholder="Informe o nome: "
                   Margin="10,0,10,0"></Entry>
            <Button Text="Salvar" Command="{Binding DefinindoNome}"
                    Margin="10,0,10,0"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>